图标文件说明

请在此文件夹中放置以下图标文件（必须是真正的 PNG 格式）：
- icon16.png   (16×16 像素)
- icon48.png  (48×48 像素)
- icon128.png (128×128 像素)

重要：仅把 .ico 改名为 .png 不会生效！
Chrome 按 PNG 格式解析图标，若文件实际仍是 ICO，会显示为灰色默认图标。
请用工具「转换」为 PNG：用画图/PS 打开 ICO 后另存为 PNG，或用在线转换（如 convertio.co/ico-png）。

若没有有效 PNG 图标，插件功能正常，但扩展列表会显示默认图标。
